{
  "name": "latvian-lang-b1",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx http-server . -p 5173",
    "test": "node --test --import ./scripts/jsdom-setup.js",
    "test:watch": "node --test --watch --import ./scripts/jsdom-setup.js",
    "validate:data": "node scripts/validate-data.mjs",
    "build:data": "node scripts/xlsx_to_json.mjs",
    "build:words:chunks": "node scripts/split-words-json.mjs",
    "build:personality": "node scripts/personality_csv_to_json.mjs",
    "build:offline": "python3 scripts/build_week1_offline.py",
    "lint": "eslint . --ext .js"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.29.1",
    "http-server": "^14.1.1",
    "jsdom": "^22.1.0",
    "prettier": "^3.3.3",
    "xlsx": "^0.18.5"
  }
}
